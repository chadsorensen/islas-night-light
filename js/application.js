// Generated by CoffeeScript 1.10.0
(function() {
  var checkTime, itsDay, itsNight, lullabyEvents;

  itsDay = function() {
    $('.night').removeClass('night');
    return $('body').addClass('day');
  };

  itsNight = function() {
    $('.day').removeClass('day');
    return $('body').addClass('night');
  };

  checkTime = function() {
    var now, time;
    now = moment();
    console.log('now', now.hour());
    return time = (function() {
      switch (false) {
        case !(now.hour() < 5 && now.hour() > 20):
          return itsNight();
        case !(now.hour() < 20 && now.hour() > 5):
          return itsDay();
        default:
          return console.log('your guess is as good as mine');
      }
    })();
  };

  lullabyEvents = function() {
    var lullaby;
    lullaby = document.getElementById("lullaby");
    return $(".music-trigger").on('click', function() {
      if (!lullaby.paused) {
        return lullaby.pause();
      } else {
        return lullaby.play();
      }
    });
  };

  $(document).ready(function() {
    var checkTimeInterval;
    checkTime();
    lullabyEvents();
    return checkTimeInterval = setInterval(checkTime, 60000);
  });

}).call(this);
